pipeline {
    agent {
        docker {
            image 'node:latest'
        }
    }
    stages {
        stage('Get Version') {
            steps {
                sh 'npm --version'
            }
        }
    }
}