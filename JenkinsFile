pipeline {
    agent {
        docker {
            image 'node:18-alpine'
            reuseNode true
        }
    }
    stages {
        stage('Get Version') {
            steps {
                sh '''
                    whoami
                    ls -al
                    node --version
                    npm --version
                '''
            }
        }
    }
}