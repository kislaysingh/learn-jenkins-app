pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello, World!'
                sh '''
                ls -la
                echo "This is a shell command execution in Jenkins pipeline."
                whoami
                '''
            }
        }
    }
}