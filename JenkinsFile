pipeline {
    agent {
        docker {
            image 'node:latest'
        }
    }
    stages {
        stage('Install npm') {
            steps {
                sh 'npm install'
            }
        }
        stage('Get Version') {
            steps {
                sh 'npm --version'
            }
        }
    }
}